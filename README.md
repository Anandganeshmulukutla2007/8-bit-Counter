# 8-bit-Counter
An 8-bit counter is a sequential digital circuit that counts clock pulses in binary form. It consists of 8 flip-flops and can represent values from 0 to 255. The counter increments its value by one on each clock pulse. A reset signal is used to initialize the count value to zero.
